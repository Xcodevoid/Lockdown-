{
  "name": "lockdown-game",
  "version": "1.0.0",
  "description": "Lockdown: Prisoners vs Guards - a real-time 2-player web game",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
